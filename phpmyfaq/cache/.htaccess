# Deny direct web access to the compiled route, container and Twig caches.
# Defence in depth for the rewrite rules in the root .htaccess.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
